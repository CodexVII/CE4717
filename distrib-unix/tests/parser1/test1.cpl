PROGRAM test;
  VAR a, b, a, c, s;

  PROCEDURE proc1 (REF a, REF b,REF c);
    VAR q, asda, v;

        PROCEDURE innerProc (REF a, REF b,REF c);
       	  VAR name, date, age;

    	  BEGIN
	    a(param1, param2);
	    b(param1, param2, param3);
	    noparam();
	    a(-b);  !'-' denotes term?
	    a(--b); !considered legal but should it be?
	    a(-1);
	    
	    exp(-(expression));	!term with expression subterm as actualParameter

	    addOp(-a + -b);	!compound term added to compound term
	  END;

    BEGIN
    END;

  PROCEDURE proc2 (REF a, REF b,REF c);
    VAR name, date, age;

    BEGIN
    END;

  BEGIN
  END.